'use client';
import React from 'react';

const HeroSlide = () => {
  return (
    <section className="bg-black h-screen w-full flex flex-col items-center justify-center text-center px-4 cursor-pointer">
      <h1 className="text-white text-2xl sm:text-3xl md:text-4xl font-light max-w-[800px] leading-relaxed">
        Evolving the drive with <span className="font-semibold">360-degree</span>  comprehensive solutions.
      </h1>
    </section>
  );
};

export default HeroSlide;
